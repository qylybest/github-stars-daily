# 复制为 .env 并填写（不要提交到 Git）
# GITHUB_TOKEN=可选，提高 GitHub API 限额

# 邮件发送为可选项；不配置 REPORT_TO 时只生成本地报告。
REPORT_TO=
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
# REPORT_FAIL_ON_EMAIL_ERROR=true
